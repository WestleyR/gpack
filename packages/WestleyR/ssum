USR_NAME="WestleyR"
PKG_NAME="ssum"
NAME="ssum"
PKG_VERSION="master"

LATEST_COMMIT=`curl -s "https://api.github.com/repos/${USR_NAME}/${PKG_NAME}/commits/master" | grep "sha" | head -1`

TARBALL="https://github.com/${USR_NAME}/${PKG_NAME}/archive/${PKG_VERSION}.tar.gz"

UNTAR_DIR="${PKG_NAME}-*"

# Load the prefix
. load_gpack ${PKG_NAME}

INSTALL_CMD="./configure --prefix ${gpack_prefix} --without-library && make install"

