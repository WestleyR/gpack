USR_NAME="WestleyR"
PKG_NAME="ssum"
NAME="ssum-lib"
PKG_VERSION="master"

TARBALL="https://github.com/WestleyR/ssum/tarball/master"
UNTAR_DIR="WestleyR-ssum-666edb9"

LIBRARIES="libssum.1.so"
HEADER_FILES="lib/ssum.1.h"

# Load the prefix
. load_gpack ${PKG_NAME}

INSTALL_LIB="make install-lib LIB_PREFIX=${gpack_prefix} INCLUDE_PREFIX=${gpack_prefix}"

